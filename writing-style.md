# 書き方メモ（hyodoblog）

過去記事から抽出した、自分の書き方の型。新しい記事を書くとき・AI に下書きさせるときの基準にする。

参照した記事:

- `f30274290511a1.md` Google Cloud 請求額が1000万円を超えた話（idea）
- `98ccf4a55b0347.md` LINE Flexカードの見切れを7回直して直らなかった話（tech）
- `f6f6edf418afab.md` Stripe Terminal AoD で3日ハマった話（tech）
- `a4c592b2b23ed7.md` 操作を全部 MCP へ移した話（tech）

## いちばん大事なこと

**報告書にしない。** 何が起きて、何を間違えて、どう抜けたかを、順番どおりに語る。

読者に渡したいのは手順ではなく、**同じ穴を踏まないための判断の履歴**。だから失敗を省略しない。むしろ失敗が本体。

## frontmatter

```yaml
---
title: "<現象>。<意外な真因や結論>"
emoji: "🐢"
type: "tech" # tech: 技術記事 / idea: アイデア
publication_name: "yoshinani_dev"
topics: ["nextjs", "supabase", "prisma", "vercel", "パフォーマンス"]
published: false
---
```

- `publication_name` は常に `yoshinani_dev`
- `topics` は 4〜5 個。サービス名・技術名を小文字で
- 書き上げた直後は `published: false`。自分で読んでから `true` にする
- タイトルは**句点で 2 文に割る**型が多い
  - 「Stripe Terminal AoDで『Could not connect to Stripe』に3日ハマりadb logcatで暴いた話」
  - 「同じSupabaseなのに管理画面が遅い。犯人はクエリではなく取りに行く場所だった」
  - 数字を入れると強い（3日、7回、1000万円）

## tech 記事の型

```
リード（3段落）
:::message（クライアント案件の断り書き）
## つくっていたもの
## 事件発生：<症状を一言で>
## <敗北 or 誤診>
## 転機：<何で抜けたか>
## <技術的な本題。h3 で分ける>
## 学び        ← h3 の番号付き。各項目は太字の教訓で締める
## おわりに
## 参考        ← 公式ドキュメントへのリンク
```

### リードの 3 段落

1. 症状を具体的に。読者が「それ知ってる」となる書き出し
2. **結論を先出しする。** 「結論から言うと、真因は〜でした」
3. 「同じ〜で詰まっている人の時短になれば嬉しいです。」で締める（ほぼ定型）

### `:::message`

クライアント案件は必ず入れる。

```
:::message
本記事は業務システムの開発に伴走した記録です。クライアント名・プロダクト名・業務固有の用語は伏せ、テーブル名などは一般的な名前に置き換えています。
:::
```

固有名詞・業務用語（社内でしか通じない単語）は全部一般名に置換する。置換したことを断り書きで明示する。

### 「つくっていたもの」

技術構成は箇条書き。ラベルを太字にする。

```markdown
- **アプリ**: Next.js 16（App Router）on Vercel
- **データ**: Supabase（Postgres）+ Prisma 7
```

### 「学び」

h3 の番号付き。各項目は「具体の説明 → 太字で一般化した教訓」の順。

```markdown
### 2. 自分の grep を疑う

（具体の説明）**測り方を疑わずに結論だけ疑うと、同じ場所をぐるぐる回ります。**
```

## 語りの作法

### 一人称と感情

「私」。感情を実況する。

- 「拍子抜けしました」「素直に嬉しかったです」「なかなか気まずいものを見つけました」
- 「ノートPCをそっと閉じ、10 分ほど散歩に出ました」

### 心の声は引用ブロック

思考の分岐点を `>` で置く。地の文で説明するより速い。

```markdown
> なるほど、ほとんどの画面がデータを待ってから描いているのか。

違いました。
```

### 自虐は具体的に

抽象的な反省を書かない。何をどう間違えたかを書く。

- 「設定を入れた時点で満足していたやつがいます。私です。」
- 「コミットメッセージにはどれも「解消」と書いてあります。そして次のコミットで再発しています。」
- 「私たちは実際にそれをやって、後から直しました。」

### タメを作る

短い断定 → 改行 → 結論。

```markdown
Supabase は東京にありました。

——**1 往復ごとに、太平洋を越えていた**わけです。
```

### 比喩は 1 記事に 1〜3 個

効かせどころだけ。多いとうるさい。

- 「エベレストの断面図」「都内にいい感じの一軒家が建ちます。ログで。」
- 「消し忘れたキャッシュは、静かに嘘をつき続けます。」

### 失敗は表にする

試行錯誤を並べるときは表。番号・やったこと・結果の 3 列。

| # | やったこと | 結果 |
| --- | --- | --- |
| 1 | 〜を削除して短縮 | 再発 |

## type による温度差

| type | 温度 | 例 |
| --- | --- | --- |
| `idea` | 振り切る。ネットミーム・ボケ・括弧の小ボケも可 | 無量空処、現場猫、「（フラグ）」 |
| `tech` | 抑える。一人称と自虐は残し、ボケは 1〜2 個まで | Stripe / LINE Flex / MCP の各記事 |

`tech` でスラップスティックにすると、技術的な信頼が落ちる。**面白さより、読者が同じ判断をできることが優先。**

## 文の作法

`.textlintrc.json` は `preset-ja-technical-writing`。守る。

- **です・ます調で統一。** である調を混ぜない
- 1 文 100 字以内
- 1 文の読点は 3 個まで
- `！` `？` を本文に使わない（心の声も `。` で終える）
- 全角数字を使わない。半角 + 前後に半角スペース（`7 回`、`16px`、`1000 万円`）
- 連続する漢字は 6 文字まで
- 「〜かもしれません」「〜と思います」は避ける。断定するか、根拠を書く

その他。

- 太字はキーフレーズだけ。1 セクションに 1〜2 個
- ダッシュは `——`（全角 2 つ）
- コードブロックには言語を指定。コメントで「なぜそうしたか」を書く

## 必ずやること

- **裏を取る。** 記憶で書かない。型定義・公式ドキュメント・実際の出力を引用する
- **測っていないことは測っていないと書く。** 「速くなったはず」を「速くなった」と書かない
- 検証に使ったコマンドをそのまま載せる（`vercel inspect`、`curl -D -`、`grep -rn`）
- 公式ドキュメントに書いてあること／書いていないことを分けて示す
- 姉妹記事があれば URL を貼る（Zenn はカード展開される）

## やらないこと

- 手順書で終わらせる。詰まった過程を省かない
- 成功だけ書く。7 回失敗したなら 7 回書く
- 固有名詞をそのまま出す
- 根拠のない断定。「〜のはずです」で逃げるくらいなら調べる

## 公開前チェック

```bash
# 表記の機械チェック
grep -n '[！？０-９]' articles/<slug>.md          # 0 件であること
grep -in 'クライアント固有語' articles/<slug>.md   # 0 件であること

# プレビュー
pnpm dev   # http://localhost:3000
```

- [ ] `published: false` のまま自分で通読した
- [ ] 結論をリードで先出ししている
- [ ] 失敗・誤診が書いてある
- [ ] 「学び」の各項目に太字の教訓がある
- [ ] 固有名詞を伏せ、`:::message` で断っている
- [ ] 数字は自分で数えた／測ったものだけ
- [ ] 参考リンクが公式を指している
