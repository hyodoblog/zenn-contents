# AGENTS.md

Zenn へ投稿する記事と本のリポジトリ。コマンド・ディレクトリ構成・記事を書くときの
決めごとは [`CLAUDE.md`](./CLAUDE.md) にまとまっている。**そちらを唯一の情報源とする。**

文体と構成の型は [`writing-style.md`](./writing-style.md) にある。記事を書く・直す前に
必ず読むこと。

特に落としてはいけない 3 点だけ、ここにも書いておく。

1. **`main` への push は公開操作。** 勝手に commit / push しない
2. **`published: false` で渡す。** 公開の判断は本人がする
3. **クライアント案件は固有名詞を伏せる。** 冒頭の `:::message` で断る
