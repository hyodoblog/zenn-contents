---
title: "同じSupabaseなのに管理画面が遅い。犯人はクエリではなく取りに行く場所だった"
emoji: "🐢"
type: "tech" # tech: 技術記事 / idea: アイデア
publication_name: "yoshinani_dev"
topics: ["nextjs", "supabase", "prisma", "vercel", "パフォーマンス"]
published: true
---

同じ Supabase、同じ Next.js 16、同じ Vercel。それなのに、片方の管理画面は開いた瞬間に表が出て、もう片方は毎回もたつきます。

結論から言うと、犯人はクエリの書き方でもインデックスの不足でもなく、**データを取りに行く場所**でした。遅いほうは、画面を 1 枚描くたびに太平洋を数往復していたのです。

同じ構成で「なぜかうちだけ遅い」と悩んでいる人の時短になれば嬉しいです。

:::message
本記事は業務システムの開発に伴走した記録です。クライアント名・プロダクト名・業務固有の用語は伏せ、テーブル名などは一般的な名前に置き換えています。
:::

## つくっていたもの

社内向けのバックオフィスです。商品マスタを管理して、原価を入力して、帳票を出す。派手さはないけれど、毎日触られる画面です。

技術構成はこんな感じです。

- **アプリ**: Next.js 16（App Router）on Vercel。React Server Components で描画
- **データ**: Supabase（Postgres）+ Prisma 7（Driver Adapters）
- **認可**: Better Auth + アプリ層のガード。全 Server Action で再検証

比較対象になったのは、同じチームが持っている別プロダクトの管理画面です。こちらは全ページ `'use client'` で、ブラウザから PostgREST を直接叩いています。

## 事件発生：隣の画面が異様に速い

「あっちの管理画面、めちゃくちゃ速いんだよね」

そう言われて開いてみたら、本当に速い。クリックした瞬間に骨組みが出て、コンマ数秒で表が埋まります。

自分たちの画面に戻ります。もたつく。同じ Supabase を見ているのに。

まず疑ったのはクエリでした。N+1 が潜んでいるとか、インデックスが効いていないとか。ところが実行計画を眺めても、テーブルの行数を数えても、責める材料が出てきません。数千行のテーブルに数ミリ秒のクエリです。犯人はここにいませんでした。

速いほうのコードを開いて、拍子抜けしました。

```tsx
export default function AdminUsers() {
  const [users, setUsers] = useState<Profile[]>([])

  useEffect(() => {
    void (async () => {
      const { data } = await supabase
        .from("profiles")
        .select("*")
        .order("created_at", { ascending: false })
        .limit(200)
      setUsers((data ?? []) as Profile[])
    })()
  }, [])
  // ...
}
```

これだけです。凝ったキャッシュも、気の利いた `Suspense` もありません。

でも、この素朴さが速さの正体でした。ページの HTML はビルド時に確定して CDN から出ます。遷移はクライアントルーティングだけで完結し、サーバー関数を一度も通りません。骨組みが即座に出て、そのあとブラウザから東京の Supabase へ 1 往復。認可は RLS が持つので、anon key をブラウザに置けます。

| | 隣の画面 | うちの画面 |
| --- | --- | --- |
| 画面の実体 | 全ページ `'use client'` | 全ページ Server Components |
| データ経路 | ブラウザ → PostgREST 直 | ブラウザ → 関数 → Prisma → Postgres |
| 認可 | RLS（DB 側） | アプリ層のガード |
| 遷移時のサーバー往復 | 0 回 | 毎回 1 回とその先の DB 往復 |

同じ Supabase を見ていても、**間に挟まっているものの数**が違いました。

## 誤診：自分の grep を信じすぎた

原因の見当がついたので、勇んで調査を始めました。そして最初に、盛大にコケます。

「ストリーミングが足りていないのでは」と考えて、こう数えました。

```bash
for f in $(find app -name "page.tsx"); do
  echo "$(grep -c '<Suspense' $f) $f"
done
```

30 ページ中、`<Suspense>` を含むのは 6 ページ。

> なるほど、ほとんどの画面がデータを待ってから描いているのか。

違いました。このリポジトリには `loading.tsx` が全ルートに置かれていて、Suspense 境界はとっくに全ページに存在していたのです。`loading.tsx` は該当セグメントを自動で Suspense に包むので、`page.tsx` を grep しても当然ヒットしません。

数字は正しかったのに、そこから引き出した結論だけが間違っていました。ここを直しても 1 ミリ秒も速くなりません。

診断をやり直します。

## 犯人：往復回数 × 1 往復のレイテンシ

数え直したのは、Suspense の数ではなく **画面 1 枚あたりの DB 往復**でした。一覧画面はこうなっていました。

1. 所属の確認（1 本）
2. 件数・種別の候補・取引先の候補（3 本を並列）
3. 一覧本体（1 本）

**クエリ 5 本、直列 3 往復。**

ここに、もうひとつの事実が重なります。`vercel.json` が無く、`preferredRegion` の指定もありませんでした。つまり Vercel の既定である `iad1`、米国東部です。

Supabase は東京にありました。

——**1 往復ごとに、太平洋を越えていた**わけです。

片道 100ms 超が、画面あたり 3 回。それだけで待ち時間が半秒に届きます。隣の画面は日本のブラウザから東京へ 1 往復なので、勝負になりません。クエリを何ミリ秒縮めたところで、埋まる差ではありませんでした。

体感速度の正体は、**1 画面あたりの往復回数 × 1 往復のレイテンシ**。この掛け算の両方に手を入れていきます。

## 打った手

### 1. リージョンを DB に寄せる

まず掛け算の右側です。Vercel のダッシュボードで Settings → Functions → Function Region を東京（`hnd1`）に変更しました。この設定は新しいデプロイから効くので、変更後にリデプロイが要ります。

効いたかどうかは、推測せずに確かめられます。ビルド成果物のリージョンは CLI で見えます。

```bash
vercel inspect https://your-app.vercel.app
```

```
├── λ index (4.82MB) [hnd1]
├── λ _global-error (4.82MB) [hnd1]
```

実際にリクエストを処理したリージョンは、レスポンスヘッダに出ます。

```bash
curl -sS -o /dev/null -D - https://your-app.vercel.app/ | grep -i x-vercel-id
```

```
x-vercel-id: hnd1::srl6v-1788069589640-4b3dc499a33a
```

`hnd1` が返ってきたときは、素直に嬉しかったです。

### 2. 往復を 3 回から 2 回へ

次は掛け算の左側です。5 本のクエリから 2 本削れました。

ひとつ目は、絞り込みプルダウンの候補です。検索語やページ番号で中身が変わりません。変わるのはマスタを書き換えたときだけです。一覧のクエリから切り離しました。

ふたつ目は、件数と本体です。互いに独立して引けるのに直列でした。並列にできない理由はページ番号の丸めで、`skip` を決めるのに総件数が要ります。

そこで、URL の値をそのまま使って先に投げ、範囲外だったときだけ引き直す形にしました。

```ts
const pageSize = resolvePageSize(query)
const requestedPage = Math.max(Math.trunc(query.page ?? 1), 1)

// 件数と本体は互いに独立して引けるので、往復を重ねずに並べて投げる
const [totalCount, requestedRowList] = await Promise.all([
  client.productMaster.count({ where }),
  findRowList(where, orderBy, requestedPage, pageSize),
])

const pagination = resolvePagination(totalCount, query)
const rowList =
  pagination.page === requestedPage
    ? requestedRowList
    : await findRowList(where, orderBy, pagination.page, pageSize)
```

範囲外のページ番号は、URL を手で書き換えたときにしか起きません。その稀なケースだけ 1 本ぶん損をして、通常時は 1 往復ぶん得をします。

結果は **クエリ 2 本、直列 2 往復**です。

### 3. `use cache` が 0 件だった

ここで、なかなか気まずいものを見つけました。

`next.config.mjs` には `cacheComponents: true` が入っています。Cache Components を有効にする設定です。ところが。

```bash
grep -rn 'use cache' --include='*.ts' --include='*.tsx' apps packages | wc -l
# => 0
```

**有効化しただけで、1 行も使っていませんでした。**

設定を入れた時点で満足していたやつがいます。私です。全ページが完全動的なまま、遷移のたびに素直に DB を引いていました。

というわけで、マスタ系の読み取りをキャッシュに載せます。

```ts
import { cacheLife, cacheTag } from "next/cache"

export const getCachedProductMasterFacet = async (organizationId: string) => {
  "use cache"
  cacheTag(productMasterCacheTag(organizationId))
  cacheLife("days")

  return getProductMasterFacet(organizationId)
}
```

無効化は書き込み側の Server Action でやります。ここで `revalidateTag` ではなく `updateTag` を選びました。

```ts
export async function saveProductMasterAction(slug: string, input: Input) {
  const membership = await requireProductMasterEditor(slug, input.productMasterId)
  // ...
  const result = await saveProductMaster(membership.organizationId, input)
  // ...
  updateTag(productMasterCacheTag(membership.organizationId))
  revalidatePath(`/organization/${slug}/product-master`)

  return { ok: true }
}
```

2 つの違いは公式ドキュメントに明記されています。

- `updateTag`: Server Action からのみ呼べる。次のリクエストは新しい値を待つ
- `revalidateTag`: Route Handler からも呼べる。古い値を返しつつ裏で更新する

保存ボタンを押したのに一覧が古いまま。業務システムでいちばんやってはいけないやつです。`updateTag` 一択でした。

### 4. コネクションプーラーを挟む

サーバーレスは実行のたびにインスタンスが増えます。直接接続だと、その都度 TCP と TLS のハンドシェイクからやり直します。HTTP の keep-alive で繋ぎっぱなしの PostgREST とは、初回コストが桁で違います。

Supabase のプーラー（transaction mode）に向けました。ここで、危うく記憶で断言しかけたことがあります。

> Prisma でプーラーを使うなら `?pgbouncer=true` を付けるんだったよな。

**付けません。** あれは旧クエリエンジン向けの指定です。念のため `@prisma/adapter-pg` の型定義を開いたら、こう書いてありました。

```ts
/**
 * Optional function to generate names for prepared statements. (中略)
 * If not provided, prepared statements are not cached.
 */
statementNameGenerator?: StatementNameGenerator;
```

`statementNameGenerator` を渡したときだけ、名前付きの prepared statement を使います。渡していなければ使わないので、transaction mode のプーラーをそのまま通せます。

「昔は必要だった指定」は、思い込みのまま何年も生き延びます。手が止まったら型定義を開くのが早いです。

なお、マイグレーション用の接続は別です。DDL はプーラー越しに流せないので、直接接続のまま置いておきます。

## キャッシュに載せる基準は「入口を数え上げられるか」

いちばん悩んだのはここです。何を載せて、何を載せないか。

遅いキャッシュより怖いのは、**古い値を平気で出し続けるキャッシュ**です。業務システムで単価や在庫が古かったら、それはバグより質が悪い。

置いた基準はひとつでした。**更新の入口を数え上げられるか。**

数え上げられるなら、そこ全部で `updateTag` を落とせます。数え上げられないなら、いつか必ず古い値が残ります。

数え上げの作業は、地味な grep の連続でした。

```bash
# 商品マスタを書き換える箇所を全部出す
grep -rn "productMaster\.\(create\|update\|delete\|upsert\|createMany\|deleteMany\)" \
  packages/features/src --include='*.ts' | grep -v spec
```

これで見落としをひとつ発見しました。**別機能の CSV 取り込みが、未登録の商品名を見つけると商品マスタ側のレコードを勝手に作っていた**のです。画面の名前からは想像もつかない経路でした。grep していなければ、確実に踏んでいます。

逆に、載せなかったものもあります。マスタ一覧に「この表を使って算出された件数」が含まれているケースです。この件数は別機能の書き込みのたびに変わります。無効化の入口が広すぎる。クエリ 1 本ぶんの得なので、割に合わないと判断して見送りました。

## 署名付き URL はキャッシュに載せられない

画像つきの一覧で、危うく踏みかけた罠です。

一覧はサムネイルを出すため、ストレージの署名付き URL を発行していました。この URL には有効期限があります。素直にキャッシュへ載せると、期限切れの URL を延々と配ることになります。

幸い、取得処理は「行を引く」と「URL を付ける」に分かれていました。そこで、**パスまでをキャッシュに載せ、URL は表示するページの分だけ毎回発行し直す**形にしました。

```tsx
// 重い問い合わせはキャッシュから。URL は毎回発行し直す
const rowList = await getCachedProductRowList(organizationId)
const { itemList, categoryList, ...page } = presentProductRowPage(rowList, query)
const productPage = {
  ...page,
  itemList: await attachThumbnailUrlList(itemList),
  categoryList,
}
```

絞り込みと並び替えも、キャッシュを跨がずにメモリ上でやります。引数を組織 ID だけにしておかないと、当たらない組み合わせが際限なく増えて、DB を引く回数が結局減りません。

## テストに止められた話

調子が出てきたので、編集画面も速くしようとしました。見出しを先に出して、データだけ後から流し込む形です。

```tsx
export default async function ProductNewPage({ params }) {
  const { slug } = await params
  return (
    <div>
      <h1>新規作成</h1>
      <Suspense fallback={<FormCardSkeleton />}>
        <FormContainer slug={slug} /> {/* ここで権限を確認 */}
      </Suspense>
    </div>
  )
}
```

テストが落ちました。

```
FAIL  keeps new, import and edit pages away from viewers
  - []
  + [ "[slug]/product/new/page.tsx" ]
```

このリポジトリには、**編集画面の `page.tsx` が権限ガードを直接含むことを検査するテスト**がありました。ガードをコンテナへ移すと、権限のない閲覧者にも一瞬「新規作成」の見出しが見えてしまいます。「権限のないユーザーには画面の存在自体を知らせない」という約束を、テストが守っていたわけです。

書いたのは過去の自分です。その自分に、速度と引き換えに約束を崩そうとしたところを止められました。

ガードはページに残し、遅いクエリだけを切り出す形に直しました。所属の確認は 1 行の問い合わせなので、直列にしても大した損はありません。

## 学び

### 1. 遅いときは、まず往復を数える

クエリを速くする前に、画面 1 枚あたりの往復回数を数えてください。数えると、リージョンの不一致もキャッシュの不在も自然に見えてきます。**体感速度は往復回数 × 1 往復のレイテンシ**で決まります。掛け算なので、片方だけ削っても効き目が半端になります。

### 2. 自分の grep を疑う

`<Suspense>` が 6 ページしかない、は事実でした。間違っていたのは、そこから引き出した結論のほうです。**測り方を疑わずに結論だけ疑うと、同じ場所をぐるぐる回ります。** 意外な数字が出たら、まずコードを開いて突き合わせるのが結局は近道でした。

### 3. 設定を入れた日に、使うところまでやる

`cacheComponents: true` を書いて満足し、`use cache` は 0 件のまま数ヶ月。よくある話だと思います。**フラグを立てたら、その日のうちに 1 箇所でいいので使ってみる。** 使えば挙動が分かり、分かれば横展開できます。

### 4. キャッシュは「消せる範囲」で設計する

載せるかどうかの判断は、速さではなく **更新の入口を数え上げられるか**で決めました。数え上げられないものは、どれだけ重くても見送っています。消し忘れたキャッシュは、静かに嘘をつき続けます。

### 5. 崩されたくない約束は、テストに書く

「編集画面の存在を閲覧者に見せない」がコメントで書いてあったら、私は踏み越えていました。実行されるテストになっていたから止まりました。**性能改善で崩されたくない設計の意図は、文章ではなくテストに置く。**

## おわりに

数字の話を最後にひとつ。この記事に、ミリ秒単位の前後比較は載せていません。数えたのは、画面あたりのクエリ本数と直列往復数だけです。

- 一覧画面: クエリ 5 本・直列 3 往復 → 2 本・2 往復
- マスタ系の読み取り: キャッシュに当たれば DB を引かない
- リージョン: `x-vercel-id` が `hnd1` であることを確認

「速くなったはず」と「速くなった」の間には、けっこうな距離があります。数えられるものだけを数えて、残りは実測に委ねることにしました。

その実測を、この記事より前にやった回があります。コードの中だけを疑って、いちばん重いクエリを 375ms から 169ms に縮めた話です。**それでも体感は変わりませんでした。** ミリ秒の前後比較は、そちらに載せています。

https://zenn.dev/yoshinani_dev/articles/e7a41c9b3d5f28

Server Components は本当に便利です。ただ、便利さの裏で往復は静かに積み上がります。もし手元のアプリがなんとなく遅いなら、`cacheComponents: true` を書いた覚えがあるかどうか、そして `use cache` を grep してみてください。0 件なら、そこがまるごと伸びしろです。

## 参考

- [Cache Components | Next.js](https://nextjs.org/docs/app/getting-started/cache-components)
- [updateTag | Next.js](https://nextjs.org/docs/app/api-reference/functions/updateTag)
- [Prisma Driver Adapters](https://www.prisma.io/docs/orm/overview/databases/database-drivers)
- [Connection pooling | Supabase](https://supabase.com/docs/guides/database/connecting-to-postgres)
